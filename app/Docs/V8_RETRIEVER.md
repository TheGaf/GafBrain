# v8 Retriever

The retriever searches the full text index and pulls only relevant conversations into a compact context pack.

## Commands

```bash
npm run recall -- "topic"
npm run context -- "question"
npm run brief -- "topic"
```

This is the bridge between archive and memory.
